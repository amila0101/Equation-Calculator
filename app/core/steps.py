def generate_steps(equation, result):
    steps = []
    steps.append(f"Equation received: {equation}")
    steps.append(f"Solving equation gives: {result}")
    steps.append("Steps generation complete")
    return steps
